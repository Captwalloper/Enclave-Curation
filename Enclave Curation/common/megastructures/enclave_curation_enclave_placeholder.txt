enclave_station_placeholder = {
	entity = "construction_platform_entity"
	construction_entity = "construction_platform_entity"
	portrait = "GFX_enclave_station_placeholder_enclave_background"
	show_in_outliner = no

	build_type = inside_gravity_well
	build_time = @curation_enclave_build_time
  
	resources = {
		category = megastructures_habitat
		cost = {
			influence = @curation_enclave_influence_cost
		}
	}

	construction_blocks_and_blocked_by = self_type
	custom_tooltip_requirements = "MEGASTRUCTURE_TOOLTIP_REQUIREMENTS_INSIDE_GRAVITY_WELL"

	potential = {
		this = { is_ai = no }
	}
	possible = {
		hidden_trigger = {
			exists = starbase
		}
		custom_tooltip = {
			fail_text = "requires_inside_border"
			is_inside_border = from
		}
	}

	on_build_complete = {
		fromfrom = { save_event_target_as = target_enclave_site }
		from = { country_event = { id = enclave_curation.100 } } 
	}
}
