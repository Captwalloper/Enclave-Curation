decision_evict_enclave = {
	sound = event_administrative_work
	icon = decision_remove_enclave
	
	resources = {
		category = decisions
		cost = {
			influence = 50 # refunded if event cancelled
		}
	}
	
	potential = {
		solar_system = {
			from = { is_ai = no }
			is_variable_set = invited_enclave_count
			check_variable = { which = invited_enclave_count value > 0 }
		}
		OR = {
			is_star = yes
			is_planet_class = pc_black_hole
			is_colony = yes
		}
	}
	
	effect = {
		solar_system = { save_event_target_as = target_enclave_system }
    from = { country_event = { id = enclave_curation.101 } } 
	}
}
