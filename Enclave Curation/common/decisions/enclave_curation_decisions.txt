#General Decision (to avoid clutter)
decision_evict_enclave = {
	sound = event_administrative_work
	icon = decision_deal
	
	resources = {
		category = decisions
		cost = {
			influence = 50
		}
	}
	
	potential = {
		this.solar_system = {
			owner = { is_ai = no }
			is_variable_set = invited_enclave_count
			check_variable = { which = invited_enclave_count value > 0 }
		}
	}
	
	effect = {
		solar_system = { save_event_target_as = target_enclave_system }
    from = { country_event = { id = enclave_curation.100 } } 
	}
}
